# =============================
# Framework Web
# =============================
fastapi==0.111.0           # Framework principal da API
uvicorn[standard]==0.30.6  # Servidor ASGI otimizado (uvloop + httptools)

# =============================
# Redis / Cache / Fila
# =============================
redis==5.1.1               # Cliente Redis moderno e performático

# =============================
# Validação / Config
# =============================
pydantic==2.7.4            # Validação de dados robusta (v2+)
python-dotenv==1.0.1       # Carregar variáveis de ambiente (.env)

# =============================
# HTTP Requests / Async
# =============================
aiohttp==3.9.5             # HTTP client assíncrono (leve e rápido)
httpx==0.27.0              # Cliente HTTP moderno com suporte a sync/async

# =============================
# Segurança / Criptografia
# =============================
cryptography==43.0.1       # Criptografia (Fernet, TLS, hashing)
python-jose==3.3.0         # JWT e assinaturas seguras

# =============================
# Observabilidade / Métricas
# =============================
prometheus-client==0.20.0  # Exposição de métricas /health e /metrics
structlog==24.4.0          # Logging estruturado (JSON logs para alta escala)

# =============================
# Enriquecimento Avançado
# =============================
geoip2==4.8.0              # Lookup GeoIP (cidade, país, ASN, lat/lon)
maxminddb==2.6.2           # Driver nativo do mmdb (performance no GeoIP2)
user-agents==2.2.0         # Parse avançado de user-agent (browser, OS, device)

# =============================
# Supervisor / Orquestração
# =============================
supervisor==4.2.5          # Gerenciador de processos (bot, worker, bridge, admin)

# =============================
# Banco de Dados (BotGestor)
# =============================
SQLAlchemy==2.0.32         # ORM moderno e performático
alembic==1.13.2            # Migrações automáticas do banco
psycopg2-binary==2.9.9     # Driver PostgreSQL (binário, produção)

# =============================
# Telegram Bot (BotGestor)
# =============================
aiogram==2.25.2            # Framework para Telegram Bot (estável)

# =============================
# Extras / Typing
# =============================
typing-extensions==4.12.2  # Tipagem moderna (compatibilidade com Pydantic v2+)